# vedit —— 项目约定

面向**酒店品牌宣传片**的视频剪辑工作流。用 YAML 描述剪辑，ffmpeg 执行。

## 这个项目服务于什么

用户经营酒店，需要**持续产出**日常宣传片（不是一次性做一条）。
视觉参考是厦门七尚酒店（LOHKAH）的品牌片：慢节奏、低饱和、大留白、
文字极克制。参考帧存在 `assets/reference/`，调色标定依赖它们，不要删。

素材约 20GB，在用户的 Mac 外接盘上，需要从中**自动挑选镜头**。

## 架构的两条主线

两套数据模型并存，各管各的场景，不要合并：

| 模块 | 模型 | 适用 |
|---|---|---|
| `timeline.py` | 一个素材 + 保留哪些段 | 口播类，去静音 |
| `shots.py` | 多镜头排列 + 溶解转场 | 品牌片（当前主线） |

## 不可退让的设计原则

**编码只发生一次。** 所有分析步骤只改数据结构，不碰画面。叠十个剪辑
步骤和只剪一刀，画质损失与耗时相同。新增步骤时不要引入中间转码。

**不装重依赖。** 全部画面指标用 ffmpeg 算，禁止引入 OpenCV / numpy。
用户在 Mac 上装这些很容易卡住，装不上就等于这套东西不存在。

**调色不用 LUT。** LUT 是黑盒，用户改不动也看不懂。每个参数都有名字，
想让绿色再灰一点就调 `saturation`。

## ffmpeg 的坑（都已踩过，别再踩）

- `drawtext` **没有字距参数**，拉开字距只能手动往字符间插空格
- 中文**必须显式传 `fontfile`**，只给家族名会渲染成方框
- 文案走 `textfile=` 而非 `text=`，绕开中文标点的转义问题
- `loudnorm` 会把采样率抬到 192kHz，后面必须跟 `aresample` 还原
- `convolution` 的矩阵**用空格分隔，不能用冒号**，冒号会被当参数分隔符
- filter_complex 里 `metadata=print:file=` 可以把多条分支的指标
  分别写到不同文件，从而**一次解码算出多个指标**

## 画质判定的经验（有实测数据）

清晰度的**绝对值不能跨镜头比较**：

```
大景深茶山空镜（对焦准）   5.84
轻微虚焦的茶山            4.48
浅景深泡茶特写（对焦准）   2.73
```

浅景深特写天生高频细节少，分数低于轻微虚焦的空镜。全局阈值会**误杀特写**，
而特写正是品牌片最需要的。所以：**镜头内比相对值找入点，绝对值只砍极端废片。**

**曝光不是排除依据。** 背景过曝成白是高调风格的刻意手法，暗调压黑同样
是风格选择。只有虚焦和晃动是客观缺陷。改这里前先读 `tests/test_library.py`。

**带黑边的素材要先 cropdetect。** 否则暗部分位数被黑边主导，一律误判死黑。

## 测试

```bash
python3 -m pytest tests/ -q
```

改动 `library.py` 的判定逻辑、`timeline.py` 的区间运算、`srt.py` 的对轴
规则时，测试必须同步更新——这三处的 bug 不会报错，只会默默出错片。

## 提交约定

提交信息用中文，说清**为什么这么改**，而不是改了哪些文件。
涉及实测数据的结论，把数据写进提交信息里。
