# 交接说明

云端会话到此为止，后续在本地继续。这份文档说明**做到哪了、为什么这么做、
下一步做什么**。先读 `CLAUDE.md` 了解项目约定。

## 在本地接上

```bash
brew install ffmpeg
pip3 install pyyaml
git clone -b claude/chat-session-ep3cfn https://github.com/onime2288-ship-it/-.git vedit
cd vedit
python3 -m pytest tests/ -q        # 应该 70 passed
```

## 需求是怎么演变的

值得记录，因为中途变过两次方向，代码里留下了痕迹：

1. 最初说「做个剪辑视频的工作流」，按**口播去静音**的方向做了骨架
2. 澄清为**酒店品牌宣传片**，去静音那套用不上，加了镜头表模型
3. 再澄清为**自动从素材库找素材**，加了索引器和联系表

所以 `timeline.py` 那套（去静音、字幕对轴）是第一阶段的产物，
对当前主线用不上，但它是完整可用的，口播类素材还能用，**先别删**。

## 已完成

| 模块 | 状态 |
|---|---|
| `library.py` | 扫描素材库、切镜头、算画质指标、推荐入点 ✅ |
| `contactsheet.py` | 生成联系表 ✅ |
| `grade.py` | 调色预设，三轮标定过 ✅ |
| `typography.py` | 中英双排文字层 ✅ |
| `shots.py` | 镜头表模型，xfade 偏移算好了 ✅ |
| `timeline.py` `render.py` `recipe.py` `srt.py` | 第一阶段产物，完整可用 ✅ |

70 个测试全过，扫描流程在真实素材上端到端验证过。

## 未完成 —— 下一步就是这个

**缺一个把镜头表渲染成片的模块**（暂定 `compose.py`）。
`shots.py` 定义了数据结构，`grade.py` 和 `typography.py` 能生成滤镜，
但**没有东西把它们拼成一条 ffmpeg 命令**。

需要做的：

1. **`brand.yml` / `film.yml` 两层配置**
   - `brand.yml`：调色预设、文字样式、转场时长、配乐规则、输出规格
     —— 定义一次，所有片子共用
   - `film.yml`：这条片子用哪些镜头、每个几秒、哪句文案
     —— 每条片子一份，十几行
   - 这个分层是「可复用模板」的核心，别合成一个文件

2. **`compose.py` 渲染器**
   - 每个镜头一个 `-ss`/`-t` 输入（输入级定位比 trim 快）
   - 单镜头链路：`setpts` → 缩放到画布 → 调色 → 信箱黑边 → 文字
   - 然后 `xfade` 链串起来，offset 用 `Sequence.offset_for()`，已经算好了
   - 配乐铺底，原声可选混入
   - 同一份剪辑渲染多种画幅（文字位置按画布高度比例自适应）

3. **配乐**：`amix` 混音、原声压低、片头片尾淡入淡出

4. **可选**：`vedit pick` —— 按规则从索引里自动初选镜头，出一版草稿

## 交接时悬而未决的事

**用户还没跑扫描。** 需要他在 Mac 上跑：

```bash
python3 -m vedit scan /Volumes/外接盘/素材库
```

生成的 `contactsheet.jpg` 要给 Claude 看，由 Claude 按编号挑镜头、定顺序、
写出 `film.yml`。**这一步必须靠看图**——指标判断不了画面内容和构图。

**调色预设还没用真实素材标定。** 当前 `outdoor` 预设是拿参考成片反推出
一份「模拟未调色原片」再套预设调出来的（对比图见
`assets/reference/调色标定-三联对比.png`），方向对但数值需要用用户的
真实素材重新标。真实素材到手后，第一件事就是重跑这个标定。

**联系表看不出运镜方向。** 静态帧只能判断「在动/不动」，判断不了推拉摇移。
排镜头顺序时，相邻镜头运镜方向冲突会很别扭，这个信息得问用户。

## 需要问用户的

- 成片时长、发布渠道（决定画幅和节奏）
- 配乐有没有选好
- 品牌 Logo 文件、要上屏的文案
- 片尾落版要不要（品牌名 + 地址，"日常宣传"场景下建议要）
